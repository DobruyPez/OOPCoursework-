<Page x:Class="_4lab.TeamPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="TeamPage" Height="400" Width="750"
      Background="#1A1A1A">

    <Page.Resources>
        <!-- Стиль для TextBlock -->
        <Style x:Key="TextBlockStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="TextAlignment" Value="Left"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для заголовков TextBlock -->
        <Style x:Key="HeaderTextBlockStyle" BasedOn="{StaticResource TextBlockStyle}" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Стиль для TextBox -->
        <Style x:Key="InputStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FF8C00"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF8C00"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FF8C00" ShadowDepth="0" BlurRadius="5" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Шаблон для Button -->
        <ControlTemplate x:Key="CustomButtonTemplate" TargetType="Button">
            <Border x:Name="ButtonBorder" 
                    CornerRadius="10" 
                    Background="#FF8C00" 
                    BorderBrush="Black" 
                    BorderThickness="1">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="ButtonBorder" 
                                               Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                               To="#4A4A4A" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetName="ButtonBorder"
                                                Storyboard.TargetProperty="Opacity"
                                                To="0.9" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation Storyboard.TargetName="ButtonBorder" 
                                               Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                               To="#FF8C00" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetName="ButtonBorder"
                                                Storyboard.TargetProperty="Opacity"
                                                To="1" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="Button.Click">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="ButtonBorder"
                                                Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)"
                                                To="0.95" Duration="0:0:0.1" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="ButtonBorder"
                                                Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)"
                                                To="0.95" Duration="0:0:0.1" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                <Border.RenderTransform>
                    <ScaleTransform CenterX="0.5" CenterY="0.5"/>
                </Border.RenderTransform>
            </Border>
        </ControlTemplate>

        <!-- Стиль для Button -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Template" Value="{StaticResource CustomButtonTemplate}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Стиль для списка участников -->
        <Style x:Key="MemberListStyle" TargetType="ListBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer>
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Margin" Value="0,2,0,2"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Border" Background="Transparent" CornerRadius="5">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#4A4A4A"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#FF8C00"/>
                                            <Setter TargetName="Border" Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect Color="#FF8C00" ShadowDepth="0" BlurRadius="5" Opacity="0.5"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для карточки участника -->
        <DataTemplate x:Key="MemberTemplate">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Ellipse Width="40" Height="40" Fill="#FF8C00" Grid.Column="0"/>
                <TextBlock Text="{Binding Name}" Style="{StaticResource TextBlockStyle}" FontSize="14" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Role}" Style="{StaticResource TextBlockStyle}" FontSize="12" Foreground="#AAAAAA" Grid.Column="2" VerticalAlignment="Center"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок команды -->
        <Border Grid.Row="0" Background="#2A2A2A" Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Название команды" Style="{StaticResource HeaderTextBlockStyle}" HorizontalAlignment="Left"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="ButtonsPanel">
                    <!-- Кнопки будут управляться из кода -->
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основное содержимое -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Список участников -->
            <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="5" Margin="10" Padding="5">
                <StackPanel>
                    <TextBlock Text="Участники" Style="{StaticResource HeaderTextBlockStyle}"/>
                    <ListBox x:Name="MembersList" 
                             ItemTemplate="{StaticResource MemberTemplate}" 
                             Style="{StaticResource MemberListStyle}" 
                             Height="300"
                             SelectionChanged="MembersList_SelectionChanged"/>
                </StackPanel>
            </Border>

            <!-- Информация о команде и управление -->
            <Border Grid.Column="1" Background="#1E1E1E" CornerRadius="5" Margin="10" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Информация о команде -->
                    <StackPanel Grid.Row="0">
                        <TextBlock Text="Информация о команде" Style="{StaticResource HeaderTextBlockStyle}"/>
                    </StackPanel>

                    <!-- Поле для редактирования описания -->
                    <TextBox x:Name="TeamDescriptionBox" Grid.Row="1" Style="{StaticResource InputStyle}" Text="Описание команды будет здесь..."/>

                    <!-- Кнопка для сохранения описания -->
                    <Button x:Name="SaveDescriptionButton" Grid.Row="2" Content="Сохранить описание" Style="{StaticResource ButtonStyle}" Click="SaveDescriptionButton_Click" HorizontalAlignment="Left" Margin="275,5,0,5" Width="180"/>

                    <!-- Управление (видно только создателю) -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" x:Name="AdminControlsPanel">
                        <Button Content="Пригласить" Style="{StaticResource ButtonStyle}" Click="InviteMemberButton_Click" x:Name="InviteMemberButton" RenderTransformOrigin="0.299,0.522" Width="120"/>
                        <Button Content="Изменить роль" Style="{StaticResource ButtonStyle}" Click="ChangeRoleButton_Click" x:Name="ChangeRoleButton" Width="144"/>
                        <Button Content="Удалить" Style="{StaticResource ButtonStyle}" Click="RemoveMemberButton_Click" x:Name="RemoveMemberButton"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>